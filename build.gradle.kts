plugins {
    id("com.android.application") version "8.5.0" apply false // Make it available but don't apply yet
    id("com.google.gms.google-services") version "4.4.2" apply false // Same for Google Services
    id("org.jetbrains.kotlin.android") version "1.9.0" apply false // And Kotlin Android
}
